#!/bin/bash

# TODO: show even more helpful information, like the other zsh git prompts

if git rev-parse --git-dir > /dev/null 2>&1
then
        echo -n "["
        git rev-parse --abbrev-ref HEAD | tr -d '\n'
        echo -n "] "
fi
